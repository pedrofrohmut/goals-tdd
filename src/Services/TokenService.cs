using Goals.Core.Services;

namespace Goals.Services;

public class TokenService : ITokenService
{
    public Task<string> CreateToken(string userId)
    {
        throw new NotImplementedException();
    }
}
